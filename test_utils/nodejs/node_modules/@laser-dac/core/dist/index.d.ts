export interface Point {
    x: number;
    y: number;
    r: number;
    g: number;
    b: number;
}
export interface Scene {
    points: Point[];
}
export declare abstract class Device {
    abstract start(): Promise<boolean>;
    abstract stop(): void;
    abstract stream(scene: Scene, pointsRate: number, fps: number): void;
}
export declare class DAC {
    devices: Device[];
    use(device: Device): void;
    remove(device: Device): void;
    removeAll(): void;
    start(): Promise<boolean>;
    stop(): Promise<void>;
    stream(scene: Scene, pointsRate?: number, fps?: number): void;
}
