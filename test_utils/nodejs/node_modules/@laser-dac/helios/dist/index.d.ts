import { Device } from '@laser-dac/core';
import * as heliosLib from './HeliosLib';
export declare class Helios extends Device {
    private interval?;
    start(): Promise<boolean>;
    stop(): void;
    private convertPoint;
    stream(scene: {
        points: heliosLib.IPoint[];
    }, pointsRate: number, fps: number): void;
}
