import { Device } from '@laser-dac/core';
import { IPoint, LasercubeScanner } from './LasercubeScanner';
import { LasercubeDevice } from './LasercubeDevice';
export declare class LasercubeWifi extends Device {
    static cmdPort: number;
    static dataPort: number;
    private cmdSocket;
    private dataSocket;
    scanner?: LasercubeScanner;
    device?: LasercubeDevice;
    search(): Promise<LasercubeDevice | null>;
    start(): Promise<boolean>;
    stop(): void;
    private startSockets;
    private stopSockets;
    private convertPoint;
    stream(scene: {
        points: IPoint[];
    }, pointsRate?: number): void;
}
