/// <reference types="node" />
import { Server as WebSocketServer } from 'ws';
import { Device, Scene } from '@laser-dac/core';
import * as http from 'http';
export declare class Simulator extends Device {
    server?: http.Server;
    wss?: WebSocketServer;
    interval?: NodeJS.Timer;
    start(): Promise<boolean>;
    stop(): void;
    private stopInterval;
    stream(scene: Scene, pointsRate?: number): void;
    private sendToSimulator;
    private sendPointsToSimulator;
    private sendPointInfoToSimulator;
}
