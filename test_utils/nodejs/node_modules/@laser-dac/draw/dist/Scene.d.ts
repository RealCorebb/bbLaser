/// <reference types="node" />
import { Point } from './Point';
import { Shape } from './Shape';
interface SceneOptions {
    resolution?: number;
}
declare type TransformFn = (points: Point[]) => Point[];
export declare class Scene {
    points: Point[];
    resolution: number;
    interval?: NodeJS.Timer;
    constructor(options?: SceneOptions);
    add(shape: Shape, transformer?: TransformFn): void;
    reset(): void;
    start(renderFrame: () => void, fps?: number): void;
    stop(): void;
    pause(): void;
}
export {};
