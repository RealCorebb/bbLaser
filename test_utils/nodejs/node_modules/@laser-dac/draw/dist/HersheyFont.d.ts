import { Shape } from './Shape';
import { Color, Point } from './Point';
interface HersheyCharacter {
    leftPos: number;
    rightPos: number;
    vertexCount: number;
    vertices: string[];
}
interface HersheyFontOptions {
    x: number;
    y: number;
    font: HersheyCharacter[];
    charWidth: number;
    spacingFactor?: number;
    text: string;
    color: Color;
}
export declare class HersheyFont implements Shape {
    private readonly font;
    private readonly charWidth;
    private readonly spacingFactor;
    private readonly x;
    private readonly y;
    private readonly text;
    private readonly color;
    constructor(options: HersheyFontOptions);
    draw(resolution: number): Point[];
}
export declare function loadHersheyFont(path: string): HersheyCharacter[];
export {};
