import { Shape } from './Shape';
import { Point, Color } from './Point';
import { SVGCommand } from 'svg-pathdata/lib/types';
interface PathOptions {
    x?: number;
    y?: number;
    width?: number;
    height?: number;
    color: Color;
    path: string;
    waitAmount?: number;
    blankingAmount?: number;
}
export declare class Path extends Shape {
    x: number;
    y: number;
    width: number;
    height: number;
    color: Color;
    path: string;
    waitAmount: number;
    blankingAmount: number;
    constructor(options: PathOptions);
    transformSize: (command: SVGCommand) => SVGCommand;
    draw(resolution: number): Point[];
}
export {};
