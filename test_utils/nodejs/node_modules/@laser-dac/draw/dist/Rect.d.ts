import { Shape } from './Shape';
import { Color } from './Point';
interface RectOptions {
    x: number;
    y: number;
    width: number;
    height: number;
    color: Color;
}
export declare class Rect extends Shape {
    x: number;
    y: number;
    width: number;
    height: number;
    color: Color;
    constructor(options: RectOptions);
    draw(resolution: number): import("./Point").Point[];
}
export {};
