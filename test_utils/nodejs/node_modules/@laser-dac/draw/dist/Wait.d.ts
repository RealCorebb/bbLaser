import { Shape } from './Shape';
import { Point, Color } from './Point';
interface WaitOptions {
    x: number;
    y: number;
    color?: Color;
    amount: number;
}
export declare class Wait extends Shape {
    x: number;
    y: number;
    color?: Color;
    amount: number;
    constructor(options: WaitOptions);
    draw(): Point[];
}
export {};
