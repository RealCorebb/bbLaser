import { Shape } from './Shape';
interface TimelineOptions {
    items: TimelineItem[];
    loop?: boolean;
}
interface TimelineItem {
    duration: number;
    render: () => Shape;
}
export declare class Timeline extends Shape {
    items: TimelineItem[];
    loop: boolean;
    private started;
    constructor(options: TimelineOptions);
    reset(): void;
    getItem(): TimelineItem | undefined;
    draw(resolution: number): import("./Point").Point[];
}
export {};
